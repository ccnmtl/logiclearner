name: build-and-test
on: [push]
jobs:
  build:
    runs-on: ubuntu-20.04
    strategy:
      matrix:
          python-version: [3.8]
          node-version: [18.x]
    name: Node ${{ matrix.node }} run
    steps:
      - uses: actions/checkout@v3
      - name: Build with Makefile
        run: make all
